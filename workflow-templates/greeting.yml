name: Greetings-New-Contributor

on:
  pull_request_target:
    types: [opened]
  issues:
    types: [opened]
  workflow_dispatch:

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
      contents: read

    steps:
      - name: Debugging Info
        run: |
          echo "Actor: ${{ github.actor }}"
          echo "Repository: ${{ github.repository }}"
          echo "Event Name: ${{ github.event_name }}"

      - name: Greet New Contributor
        uses: actions/first-interaction@v1.1.1
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          issue-message: |
            👋 Hello! @${{ github.actor }} Welcome to ${{ github.repository }}. 

            We're excited to have you here! Please make sure to check out our [Contribution Guide](https://github.com/${{ github.repository }}/blob/main/CONTRIBUTING.md) and feel free to join our community discussions for any questions.

          pr-message: |
            👋 Hello! @${{ github.actor }} Welcome to ${{ github.repository }}. 

            Thank you for submitting your first Pull Request! We are delighted to have you in our community!
            Please make sure to check out our [Contribution Guide](https://github.com/${{ github.repository }}/blob/main/CONTRIBUTING.md) and feel free to join our community discussions for any questions.
